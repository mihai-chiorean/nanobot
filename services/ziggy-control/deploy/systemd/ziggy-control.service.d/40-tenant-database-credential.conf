[Service]
# Install this drop-in only when ZIGGY_TENANCY_MODE=postgres. Manifest mode
# keeps using its files and does not require this credential.
LoadCredential=tenant-database-url:/etc/ziggy/secrets/tenant-database-url
Environment=ZIGGY_TENANT_DATABASE_URL_FILE=%d/tenant-database-url
